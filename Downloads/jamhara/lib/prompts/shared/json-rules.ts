/**
 * JSON_OUTPUT_RULES — قواعد إخراج JSON
 * ───────────────────────────────────────
 * VERSION : 1.0
 * UPDATED : مارس 2026
 * SCOPE   : تُلحق بنهاية أي برومبت يتوقع JSON نظيفاً
 *
 * هذه القواعد تعالج أكثر أسباب فشل التوليد شيوعاً:
 *   - نص قبل/بعد JSON
 *   - وسوم HTML داخل النصوص
 *   - JSON مع markdown fences
 */

export const JSON_OUTPUT_RULES = `
**قواعد الإخراج — إلزامية:**
- أرجع JSON صالحاً فقط — بدون أي نص قبله أو بعده
- بدون markdown، بدون \`\`\`json، بدون تعليقات
- كل الحقول النصية: نص عادي بلا HTML أو وسوم أو citation tags من أي نوع
- تحقق من صحة بنية JSON قبل الإرسال`;
